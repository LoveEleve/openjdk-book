# E-1a Engine 写入路径 · 六层深度审查报告

> 审查基线：E-1a 四件套，ES v8.12.2 源码
> 审查日期：2026-08-21

---

## 1️⃣ 事实审

| 锚点 | 源码行 | 结果 |
|------|:------:|:----:|
| `InternalEngine.java:1131` index() | `InternalEngine.java:1131` | ✅ |
| `InternalEngine.java:1314` planIndexingAsPrimary() | `InternalEngine.java:1314` | ✅ |
| `InternalEngine.java:1384` indexIntoLucene() | `InternalEngine.java:1384` | ✅ |
| `InternalEngine.java:1059` canOptimizeAddDocument() | `InternalEngine.java:1059` | ✅ |
| `InternalEngine.java:1483` IndexingStrategy 类 | `InternalEngine.java:1483` | ✅ |
| `Engine.java:428` 抽象 index() | `Engine.java:428` | ✅ |
| `LiveVersionMap.java:337` maybePutIndexUnderLock() | `LiveVersionMap.java:337` | ✅ |

### 关键断言核实

| 断言 | 源码证据 | 成立 |
|------|---------|:----:|
| `canOptimizeAddDocument` 用 `autoGeneratedIdTimestamp` 检测重试 | `InternalEngine.java:1060` `if (index.getAutoGeneratedIdTimestamp() != UNSET...)` | ✅ |
| `planIndexingAsPrimary` 先检查 `canOptimizeAddDocument` | `InternalEngine.java:1319` `boolean canOptimize = canOptimizeAddDocument(index)` | ✅ |
| `IndexingStrategy` 含 `indexIntoLucene/appStaleOpToLucene/useLuceneUpdateDocument` | `InternalEngine.java:1483` 类定义 + `:1195`/`:1205`/`:1397` 使用 | ✅ |

**7 个锚点全部通过，无事实错误。**

---

## 2️⃣ 因果审

- `planIndexingAsPrimary` 的 plan-and-execute 模式先判断策略后执行 ✅
- `canOptimizeAddDocument` 用 autoGeneratedIdTimestamp 跳过版本查找，提升 append-only 性能 ✅
- `LiveVersionMap` + `readLock` 允许多 index 并发写入 ✅

## 3️⃣ 结构审

- 从"新增还是更新"困惑开场 → `planIndexingAsPrimary` → `canOptimizeAddDocument` → `indexIntoLucene` → `LiveVersionMap`，主线集中 ✅

## 4️⃣ 读者审

- 读完能回答：InternalEngine 怎么决定新增还是更新 ✅
- 读完能回答：canOptimizeAddDocument 怎么优化 append-only 场景 ✅

## 5️⃣ 边界审

- 不展开 Translog.add() 的 WAL 写入细节（E-3 覆盖）✅
- 不展开 fetchPhase 的完整查询路径（E-2a 覆盖）✅

## 6️⃣ 依赖审

- 前置 E-3，后续 E-1b ✅

---

## 结论

| 审层 | 结果 |
|:----:|:----:|
| 事实审 | ✅ 7 锚点全部通过 |
| 因果审 | ✅ |
| 结构审 | ✅ |
| 读者审 | ✅ |
| 边界审 | ✅ |
| 依赖审 | ✅ |

E-1a 通过六层审查，无修正，可进入 E-1b refresh/flush 可见性。
ENDOFFILE