# 第6章：compilationPolicy_init — 编译策略选择与阈值

> `init_globals()` 第 106 行调用 `compilationPolicy_init()` → 选择编译策略 + 调用 `initialize()` 计算 CICompilerCount。本章展开策略选择机制、CICompilerCount 双公式 ergonomics、和非 Tiered / Tiered 两套编译阈值体系。

## 子节

| 节 | 文件 | 内容 |
|----|------|------|
| 6.1 | [01-policy-selection.md](ch06/01-policy-selection.md) | 策略选择 + CICompilerCount ergonomics — `CompilationPolicyChoice` switch 分发、三种策略差异（Simple/StackWalk/Tiered）、双公式（`log2(n)-1` vs `log n*loglog n*3/2`）、C1:C2 拆分、code cache 容量约束、32 位强制 3 线程、InlineSmallCode 平台调优 |
