# 第14章：G1Policy + ConcurrentMark —— GC 调度基础设施

> 本章讲解 universe_init 中最后两个子系统：G1Policy（IHOP 预测、Region 选择策略）和 ConcurrentMark（双 Bitmap、SATB 队列）。这两个系统在 GC 触发时才真正"工作"，但 init_globals 中的初始化决定了它们的初始参数。

> 下一章进入 init_globals Phase 3——universe_init 创建了堆和 GC 基础设施，现在要在堆之上构建字节码执行层。

TODO
