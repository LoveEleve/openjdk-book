# 第16章：TemplateInterpreter —— 286 个 Codelet 的生成

> 本章拆解 interpreter_init() = TemplateInterpreter::initialize()：~256KB 的 StubQueue、CodeletMark RAII 提交机制、generate_all() 的分类策略、TosState 11 种类型映射。256 个字节码每个都是一个 ~100 字节的汇编片段。

TODO
