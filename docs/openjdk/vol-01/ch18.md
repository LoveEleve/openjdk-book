# 第18章：那些微小的初始化——sizeof 断言、GC 种子、寄存器名

> 本章合并三个体量最小的初始化：accessFlags_init（sizeof(AccessFlags) == sizeof(jint) 断言）、InterfaceSupport_init（固定 GC 随机种子）、VMRegImpl::set_regName（569 条寄存器名映射）。它们单独成章太单薄，合在一起就是 init_globals 的"琐碎但必要"的侧面。

TODO
