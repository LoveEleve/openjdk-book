# 第9章：stubRoutines_init — 运行时 Stub 桩生成

> `init_globals()` 第 110 行调用 `stubRoutines_init1()` -> `StubRoutines::initialize1()`。本章从最基础的知识点出发，逐层往上建——从 C 语言的 `static address` 函数指针开始，一直到 call_stub 的 342 行 x86 汇编逐指令拆解。

## 知识点链（从底层往上读）

| 顺序 | 文件 | 层 | 解决什么 |
|------|------|----|---------|
| 1 | [01-stubroutines-bufferblob.md](ch09/01-stubroutines-bufferblob.md) | 1-3 | StubRoutines 全局函数指针表、BufferBlob 内存容器、CodeBlobLayout 坐标系统 |
| 2 | [02-code-writing.md](ch09/02-code-writing.md) | 4-7 | 怎么往 BufferBlob 里写 x86 机器码——CodeSection 三指针 → Assembler 指令编码 → CodeBuffer 多段管理 → `__` 宏 |
| 3 | [03-stubgenerator.md](ch09/03-stubgenerator.md) | 8-10 | StubGenerator 类、generate_initial 的 17+ 个核心桩、StubCodeMark |
| 4 | [04-call-stub.md](ch09/04-call-stub.md) | 11-13 | C/entry/Java 三种帧类型、call_stub 的 342 行 x86 汇编逐指令拆解 |
