# 第7章：codeCache_init — code cache 初始化

> `init_globals()` 第 107 行调用 `codeCache_init()` -> `CodeCache::initialize()`。本章展开 code cache 的三段堆架构、内存预留（mmap）、指令缓存刷新机制。

## 子节

| 节 | 文件 | 内容 |
|----|------|------|
| 7.1 | [01-heap-layout.md](ch07/01-heap-layout.md) | 三段堆架构 + 内存预留 + ICache flush — 代码缓存概念、NonNMethod/Profiled/NonProfiled 三段布局、编译器缓冲预留、reserve_heap_memory mmap、icache_init 指令缓存刷新 |
