# 第22章：vmStructs + PrintFlags —— SA 调试与配置归档

> 本章讲解 vmStructs_init() 和 JVMFlag::printFlags：三个宏（VM_STRUCTS_DO/VM_TYPES_DO/VM_INT_CONSTANTS_DO）如何把 C++ 结构体布局导出给 Java 层 SA 代理，支撑 jstack/jmap/jinfo 的底层原理。PrintFlags 为什么在 init_globals 最后才打印（需要所有 VM_Version/os 调整完成后）。

TODO
