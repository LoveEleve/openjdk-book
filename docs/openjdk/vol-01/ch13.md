# 第13章：LatestMethodCache —— 6 个单槽方法缓存

> 本章承接 `universe_init` 中的 6 个 `LatestMethodCache` 创建。每个缓存只有两个字段（`_klass` + `_method_idnum`），在类还没加载时就预先创建空壳，首次使用时延迟填充——用 `method_idnum` 而不是 `Method*` 指针保证 RedefineClasses 安全。

## 子节

| 节 | 文件 | 内容 |
|----|------|------|
| 13.1 | [01-latest-method-cache.md](ch13/01-latest-method-cache.md) | LatestMethodCache 全线：空壳创建 + 延迟填充 + idnum 安全 + 6 个场景 |

**前置知识**：[ch10/07 Metaspace 背景知识](../ch10/07-metaspace.md)——理解 Method、Klass、method_idnum。

**写作规划**：[PLAN.md](ch13/PLAN.md)
