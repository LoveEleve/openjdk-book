# R-4 SDS 字符串 · 六层深度审查报告

> 审查基线：R-4 四件套全部文件（rewrite-plan / 正文 / note / review-notes），Redis 7.4.2 源码
> 审查日期：2026-08-21

---

## 1️⃣ 事实审

### 锚点逐行核实

| 正文行 | 引用锚点 | 源码行 | 结果 |
|-------|----------|:------:|:----:|
| 26 | `src/sds.h:20` `typedef char *sds;` | `sds.h:20` | ✅ |
| 32 | `src/sds.h:61` `SDS_HDR` 宏 | `sds.h:61` | ✅ |
| 51 | `src/sds.h:24-47` 5 种 sdshdr 结构体 | `sds.h:24-47` | ✅ |
| 52 | `src/sds.h:53-57` SDS_TYPE_5/8/16/32/64 | `sds.h:53-57` | ✅ |
| 80 | `src/sds.h:62` `SDS_TYPE_5_LEN` | `sds.h:62` | ✅ |
| 80 | `src/sds.c:245-246` 升级逻辑 | `sds.c:244` | ⚠️ 行号偏移（正文 245-246 实际为 244，卷级总审复核） |
| 84 | `sdsReqType()` 14 行 inline | `sds.c:38-52` | ✅ 实际 15 行，正文说约 14 行，可接受 |
| 96 | `src/sds.c:81` `_sdsnewlen()` | `sds.c:81` | ✅ |
| 102 | `src/sds.h:60` `SDS_HDR_VAR` 宏 | `sds.h:60` | ✅ |
| 108 | `sizeof(sdshdr8)=3` 字节 | `sds.h:28-31` | ✅ 1+1+1=3 |
| 122 | `src/sds.c:217-260` `_sdsMakeRoomFor()` | `sds.c:217-260` | ✅ |
| 115 | `if (type == SDS_TYPE_5) type = SDS_TYPE_8` | `sds.c:244` | ✅ 实际行号 244 |

> 注：本 v6-review 初稿曾误写为 `238-239`，卷级总审复核确认为 `sds.c:244`，已同步修正正文与 review-notes。
| 141 | `SDS_MAX_PREALLOC = 1024*1024` | `sds.h:13` | ✅ |
| 147 | `src/sds.c:463-469` `sdscatlen()` | `sds.c:463-469` | ✅ |
| 108 | `__packed__` 取消对齐 | `sds.h:24-47` | ✅ 每个 sdshdr 前都有 `__attribute__((__packed__))` |

### 发现 1 个行号偏差

**问题**：正文第 80 行引用 `src/sds.c:245-246` 作为 `if (type == SDS_TYPE_5) type = SDS_TYPE_8` 的位置。实际行号是 `sds.c:244`。卷级总审复核确认。

**影响**：低。行号偏移 6 行，不影响源码阅读，但需要修正以保持精确。

### 其余 14 个锚点全部精确

无其他问题。

---

## 2️⃣ 因果审

| 正文主张 | 源码证据 | 成立 |
|---------|---------|:----:|
| `sds = char *` 指向 buf，header 紧贴 buf 前面 | `sds.h:20` + `SDS_HDR` 宏（`sds.h:61`） | ✅ |
| 5 种 header 覆盖不同长度区间 | `sds.h:24-47` 结构体定义 | ✅ |
| `__packed__` 取消对齐，确保精确 header 大小 | `sds.h:24-47` `__attribute__((__packed__))` | ✅ |
| `sdshdr5` 没有 alloc 字段不能用于追加 | `sds.h:24-26` 只有 flags + buf | ✅ |
| 预分配 <1MB 翻倍、≥1MB +1MB | `sds.c:233-236` | ✅ |
| `sdscatlen` 用显式 memcpy 长度实现二进制安全 | `sds.c:463-469` memcpy 用 len 参数 | ✅ |

---

## 3️⃣ 结构审

正文结构：
```
困惑开场 → 内存布局 → 5 种 header → __packed__ → sdshdr5 陷阱 → 预分配 → 二进制安全 → 扩容升级 → 失败路径 → 收网
```

- 从"为什么 char * 不够"到"带 header 的连续内存块"，主线集中
- 每节只回答一个问题，没有跨节重复
- 失败路径 OOM / size_t 溢出 / header 类型漂移 3 个场景覆盖了主要风险

**结构完整，无冗余。**

---

## 4️⃣ 读者审

### 前置知识检查

- 需要知道 C 语言的 `struct`、`char *`、`memcpy` 等基础
- 不需要知道 Redis 其他数据结构
- 前置依赖 R-1（redisObject）已提供 EMBSTR/RAW 编码的上下文

### 读者应能回答的问题

| 问题 | 正文中是否有答案 |
|------|:--------------:|
| 为什么 sdshdr5 追加时自动升级？ | ✅ 第 80 行 + 第 110-118 行 |
| 预分配为什么是 <1MB 翻倍而 ≥1MB +1MB？ | ✅ 第 131-135 行 + 第 141 行 |
| SDS 为什么能存二进制数据？ | ✅ 第 146-160 行 |
| `__packed__` 解决了什么问题？ | ✅ 第 98-108 行 |
| 5 种 header 各管多长字符串？ | ✅ 第 72-78 行 |

---

## 5️⃣ 边界审

### 与相邻域的边界

| 相邻域 | 边界处理 | 是否清晰 |
|--------|---------|:--------:|
| R-1 redisObject（EMBSTR/RAW 编码） | 正文只在第 197 行提到 SDS 是 EMBSTR/RAW 的载体，不展开 EMBSTR 细节 | ✅ |
| R-3 Dict | 正文不展开 rehash、SipHash 等 Dict 内容，只是下篇桥接 | ✅ |
| R-25 缓冲区 | 正文不展开 querybuf 等缓冲区内容 | ✅ |

### 自身边界

- 不展开 `sdsfree`/`sdscpy`/`sdscmp` 等辅助函数
- 不展开 C 字符串的 `strlen`/`strcat` 安全性问题对比（只点了关键差异）

---

## 6️⃣ 依赖审

| 依赖 | 方向 | 是否满足 |
|------|------|:--------:|
| R-1 redisObject → R-4 SDS | 阅读前提 | ✅ R-1 已完成 |
| R-4 SDS → R-3 Dict | 后续桥接 | ✅ 下篇桥接指向 R-3 |
| R-4 SDS → R-25 缓冲区 | 无依赖 | ✅ 不涉及 |

---

## 结论

| 审层 | 结果 | 备注 |
|:----:|:----:|------|
| 事实审 | ⚠️ 通过（有 1 个行号偏差） | 正文第 80 行 `sds.c:245-246` 应为 `sds.c:244`（卷级总审复核确认） |
| 因果审 | ✅ 通过 | 所有主张均有源码证据 |
| 结构审 | ✅ 通过 | 主线集中，无冗余 |
| 读者审 | ✅ 通过 | 前置知识合理，关键问题有答案 |
| 边界审 | ✅ 通过 | 与相邻域边界清晰 |
| 依赖审 | ✅ 通过 | 无循环依赖 |

### 需要修正的 1 个问题

**正文第 80 行**：`src/sds.c:245-246` → 改为 `src/sds.c:244`（卷级总审复核确认行号）

修正后即可进入 R-3 Dict。
