# R-11/12/13 + R-8 + R-9 + R-14 + R-15 批量六层深度审查报告

> 审查基线：R-11/12/13 专题、R-8/9/14/15 四件套全部文件，Redis 7.4.2 源码
> 审查日期：2026-08-21

---

## 1️⃣ 事实审

### 锚点批量核实

| 域 | 锚点数 | 核实 |
|:--:|:------:|:----:|
| R-11/12/13 Bitmap/HLL/GEO | 12 | ✅ 全部通过 |
| R-8 持久化 | 10 | ✅ 全部通过（1 处过时表述修正） |
| R-9 复制 | 6 | ✅ 全部通过 |
| R-14 Sentinel | 6 | ✅ 全部通过 |
| R-15 Cluster | 5 | ✅ 全部通过 |
| **总计** | **39** | **✅ 39 通过 + 1 修正** |

### 发现并修正的 1 个问题

**R-8 AOF Rewrite 机制描述过时**：正文写"fork 子进程写全量 + pipe 传增量"（Redis 6.x 模型），但 Redis 7.x 的 Multi-part AOF 用 `openNewIncrAofForAppend()`（`aof.c:771`）在 rewrite 前打开一个新的 incremental.aof 文件累积增量，子进程写出 base.rdb，主进程继续写 incremental.aof。已修正正文 + note + review-notes。

---

## 2️⃣ 因果审

| 域 | 因果链 | 成立 |
|:--:|--------|:----:|
| R-11/12/13 | 三个扩展编码都借用现有结构做编码（Bitmap 借 SDS、HLL 借字符串、GEO 借 ZSet） | ✅ |
| R-8 | RDB 点快照 + AOF 线日志互补，Multi-part 融合两者 | ✅（修正后） |
| R-9 | 全量同步一次 + PSYNC 增量 backlog 环形缓冲 | ✅ |
| R-14 | SDOWN 单哨兵判断 + ODOWN 多哨兵投票 + 故障转移三阶段 | ✅ |
| R-15 | CRC16 % 16384 映射 slot + Gossip 最终一致拓扑 + 投票选主 | ✅ |

---

## 3️⃣ 结构审

5 个域的结构均符合方法论：困惑开场 → 核心概念 → 分层拆解 → 失败路径 → 收网。无冗余。

## 4️⃣ 读者审

- 读完能回答：Bitmap/HLL/GEO 的公共设计模式 ✅
- 读完能回答：RDB 和 AOF 各自解决什么问题 ✅
- 读完能回答：PSYNC 部分同步怎么工作 ✅
- 读完能回答：SDOWN 和 ODOWN 区别 ✅
- 读完能回答：Cluster 怎么路由 key 到节点 ✅

## 5️⃣ 边界审

- 各域只覆盖本域内容，未透支后续域，边界清晰。

## 6️⃣ 依赖审

- 依赖链单向无回环：R-1/6 → R-11/12/13；R-8(S←R-2) → R-9(S←R-8) → R-14(S←R-9) → R-15(S←R-9/R-27)

---

## 结论

| 审层 | 结果 |
|:----:|:----:|
| 事实审 | ✅ 39 锚点通过 + 1 修正 |
| 因果审 | ✅ |
| 结构审 | ✅ |
| 读者审 | ✅ |
| 边界审 | ✅ |
| 依赖审 | ✅ |

**5 个域全部通过深度复审。** vol-redis 已完成 22 个域，剩余 10 域（R-19~R-24 排障层 + R-16/R-17/R-18 高级特性）。
