# R-28 ~ R-32 批量六层深度审查报告

> 审查基线：R-28~R-32 五件四件套全部文件，Redis 7.4.2 源码
> 审查日期：2026-08-21

---

## 1️⃣ 事实审

### 锚点批量核实

| 域 | 锚点数 | 核实 |
|:--:|:------:|:----:|
| R-28 内存淘汰 | 8 | ✅ 全部通过（1 处行号修正） |
| R-29 键空间与 SCAN | 7 | ✅ 全部通过 |
| R-30 阻塞命令 | 5 | ✅ 全部通过 |
| R-31 发布订阅 | 4 | ✅ 全部通过 |
| R-32 ACL | 5 | ✅ 全部通过（1 处函数名修正） |
| **总计** | **29** | **✅ 29 通过 + 2 修正** |

### 发现并修正的 2 个问题

1. **R-28 `performEvictions` 行号偏差**：正文写 `evict.c:567`，实际定义在 `evict.c:520`。已修正正文 + review-notes。

2. **R-32 ACL 检查函数名错误**：正文写 `ACLCheckAllCommand`，实际函数是 `ACLCheckAllPerm`（`server.c:3987`，在 `authRequired` 之后、`call` 之前调用）。已修正正文 + review-notes。

---

## 2️⃣ 因果审

| 域 | 因果链 | 成立 |
|:--:|--------|:----:|
| R-28 | 采样 + 候选池替代全局链表，避免高并发锁竞争 | ✅ |
| R-29 | kvstore 多 dict 支持 cluster 槽位分片 | ✅ |
| R-30 | blockForKeys 注册 → signalKeyAsReady 标记 → handleClientsBlockedOnKeys 处理 | ✅ |
| R-31 | 广播遍历 pubsub_channels/patterns，不持久化所以易失 | ✅ |
| R-32 | authRequired 认证 + ACLCheckAllPerm 命令权限双检查 | ✅（修正后） |

---

## 3️⃣ 结构审

5 个域均符合方法论结构：困惑开场 → 核心概念 → 分层拆解 → 失败路径 → 收网。

## 4️⃣ 读者审

- R-28：为什么是近似 LRU？✅
- R-29：KEYS 为什么阻塞而 SCAN 不阻塞？✅
- R-30：BLPOP 挂起后为什么不占 CPU？✅
- R-31：Pub/Sub 消息为什么丢？✅
- R-32：default 用户有什么权限？✅

## 5️⃣ 边界审

- 各域只覆盖本域内容，未透支后续域（R-26→R-32 边界清晰）。

## 6️⃣ 依赖审

- 依赖链单向无回环：R-28(S←R-1/R-23) → R-29(S←R-3/R-26) → R-30(S←R-2/R-5) → R-31(S←R-25) → R-32(S←R-26)。

---

## 结论

| 审层 | 结果 |
|:----:|:----:|
| 事实审 | ✅ 29 锚点通过 + 2 修正 |
| 因果审 | ✅ |
| 结构审 | ✅ |
| 读者审 | ✅ |
| 边界审 | ✅ |
| 依赖审 | ✅ |

**D 层（经典混淆补深 R-28~R-32）全部通过深度复审。** 可继续 R-11 Bitmap/HLL/GEO。
