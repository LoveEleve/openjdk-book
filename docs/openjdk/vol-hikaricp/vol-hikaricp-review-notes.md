# vol-hikaricp 卷级六层审查笔记

> 审查基线：HikariCP 7.0.2 当前源码；正文 9 篇；对照卷 `vol-druid`。

## 结论

卷级审查完成。正文锚点已扫描并复核，所有引用对应的源码文件存在，行号均在当前文件范围内。物理目录不移动，推荐阅读顺序为 `Ch1 -> Ch2 -> Ch3 -> Ch4 -> Ch5 -> Ch6 -> Ch7 -> Ch8 -> Ch9`。

## 一、事实审

- 扫描 9 篇正文，共约 90 个 `file:line` 引用。
- 所有引用对应的源码文件存在，行号均在当前文件范围内。
- 未发现跨篇重复锚点。
- 源码抽查确认：
  - `HikariPool.java:140` 为 `getConnection()` 入口，`:152` 为 `getConnection(long)`，正文成立。
  - `ConcurrentBag.java:132` 为 `borrow()` 方法，`:134`/`:148`/`:163` 为三段路径，正文成立。
  - `ProxyConnection.java:240` 为 `close()` 方法，`:57`/`:254` 为 dirtyBits，正文成立。
  - `PoolBase.java:157` 为 `isConnectionDead()` 方法，`:160`/`:177` 为 networkTimeout 设置，正文成立。
  - `HikariPool.java:485` 为 `createPoolEntry()` 方法，`:493`/`:495`/`:498`/`:501`/`:503` 为任务调度，正文成立。

## 二、因果审

跨篇链路成立：

- `Ch1 -> Ch2`：总骨架立住后，借出链才能展开。
- `Ch2 -> Ch3`：借出后半链入口是归还，两条链前后衔接。
- `Ch2/Ch3 -> Ch4`：借还与 ConcurrentBag 存储层紧密协作。
- `Ch3 -> Ch5`：归还后连接进入生命周期管理，HouseKeeper 后台维护。
- `Ch2 -> Ch6`：借出后的验证决定是否能交付。
- `Ch2 -> Ch7`：借出时埋下泄漏诊断任务。
- `Ch5/Ch6 -> Ch8`：后台维护和验证需要 JMX 控制面观察。
- `Ch8 -> Ch9`：JMX 控制面与 metrics 观测面互补。

## 三、结构审

9 篇按两层组织：
- 主干层（Ch1~Ch5）：架构、借出、归还/驱逐、ConcurrentBag、生命周期
- 诊断与可观测层（Ch6~Ch9）：验证、泄漏检测、JMX、metrics

物理目录 `ch1` 到 `ch9` 与推荐阅读顺序一致，无目录重命名需求。

## 四、读者审

- Ch1 不依赖任何前篇，是入口。
- Ch2 依赖 Ch1 的骨架；满足。
- Ch3 依赖 Ch2 的前半链；满足。
- Ch4 依赖 Ch2/Ch3 的借还语义；满足。
- Ch5 依赖 Ch3 的归还语义；满足。
- Ch6 依赖 Ch2 的借出语义；满足。
- Ch7 依赖 Ch2 的借出语义；满足。
- Ch8 依赖 Ch5/Ch6 的后台语义；满足。
- Ch9 不依赖前篇具体机制，但建议在主干层之后阅读；满足。
- 未发现"前篇尚未建立却被后篇当作已知"的循环依赖。

## 五、边界审

边界总体清晰：
- Ch1 讲总骨架，不提前讲借出/归还细节。
- Ch2 讲借出，不提前讲 ConcurrentBag 内部。
- Ch3 讲归还/驱逐，不提前讲 HouseKeeper。
- Ch4 讲 ConcurrentBag，不提前讲验证/泄漏。
- Ch5 讲生命周期，不提前讲验证细节。
- Ch6 讲验证，不提前讲泄漏检测。
- Ch7 讲泄漏检测，不提前讲 JMX。
- Ch8 讲 JMX，不提前讲 metrics。
- Ch9 讲 metrics，不提前讲 JMX 控制面。

## 六、依赖审

- 依赖方向为骨架 -> 借出 -> 归还 -> 存储层 -> 生命周期 -> 验证 -> 诊断 -> 控制 -> 观测，无回环。
- 与 `vol-druid` 的关系：两卷以对照关系存在，HikariCP 是 Druid 的对照基准。两卷都覆盖了连接池核心机制（借出、归还、验证、维护），但设计差异大，互不冲突。

## 修正记录

无。所有行号在写作时已核对。

## 验证记录

- 正文锚点脚本检查：9 篇共约 90 个引用，坏引用 0 个。
- HikariCP 源码版本 7.0.2。
- 该文档目录无独立 lint/typecheck 配置；本次以锚点扫描、源码上下文复核和 Markdown 结构检查作为等价校验。
