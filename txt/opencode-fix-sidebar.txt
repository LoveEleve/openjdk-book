两个任务:

## 任务1: 修复侧边栏箭头

docs/index.html 第32行起有一段"侧边栏箭头统一"的CSS块，把原生 sidebar 箭头全藏了又试图自己画▶，结果是箭头不显示但有残留样式。

把这个CSS块彻底删掉（从注释"侧边栏箭头统一"开始到暗色模式箭头结束），只保留一句:
```
.sidebar { font-family: var(--body-font) !important; }
```

让 sidebar-collapse 插件的原生样式正常工作，不要再有任何自定义箭头CSS。

## 任务2: 删除 Excalidraw 和 Draw.io 代码

从以下四个文件中删除所有 excalidraw / drawio 相关代码:
- docs/index.html: CSS样式块 + CDN脚本(pako,docsify-drawio) + JS中的drawioRenderer/enhanceDrawioElements/enhanceExcalidrawElements
- docs/assets/common.css: drawio-container相关
- docs/assets/common.js: markdown renderer中的drawio分支 + enhanceDrawioElements + doneEach调用
- index.html: CDN脚本 docsify-drawio

改完提交推送。
