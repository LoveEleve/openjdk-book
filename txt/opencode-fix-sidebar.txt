两个任务：

## 任务1: 继续修复搜索 — 目前仍不可用

先检查当前线上 common.js 的实际搜索逻辑：
- Ctrl+K 能否打开搜索面板
- 点击右下角搜索按钮能否打开搜索面板
- 输入关键字后是否有结果

如果还有问题，深入排查 buildSearchIndex / loadSearchIndex / performSearch 完整流程。需要让搜索真正可用。

## 任务2: 去掉左边侧边栏

既然侧边栏只剩"首页"一个链接，直接关掉让内容区全宽展示更好。

具体改动:
1. docs/index.html 和 index.html 中设置 `loadSidebar: false`
2. 检查 common.js 中依赖侧边栏 .sidebar-nav 的代码（renderBreadcrumbAndNav 的上一篇/下一篇、buildSearchIndex 的链接扫描等），改为从 .markdown-section 或 _sidebar.md 文件内容中获取链接
3. docs/index.html 和 common.css 中删除侧边栏相关 CSS 样式（sidebar-width、sidebar 背景色等）
4. common.css 中 content-max-width 从小宽度改为更舒适的全宽值（如 900px 或 max-width: 100%）
5. 暗色模式下也不再需要 sidebar 样式

改完验证搜索功能，然后提交推送。
